Revisiting DQN

• Approximate $Q^*(s, a)$ by a neural network, $Q(s, a; \mathbf{w})$.

state $s$ → Conv → feature → Dense → $Q(s, \text{"left"}; \mathbf{w})$
                                    $Q(s, \text{"right"}; \mathbf{w})$
                                    $Q(s, \text{"up"}; \mathbf{w})$