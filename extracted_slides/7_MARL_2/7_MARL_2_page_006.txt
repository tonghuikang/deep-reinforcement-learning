Fully Decentralized Execution

$a^1 \sim \pi(\cdot \vert o^1; \boldsymbol{\theta}^1)$    $a^2 \sim \pi(\cdot \vert o^2; \boldsymbol{\theta}^2)$    $a^n \sim \pi(\cdot \vert o^n; \boldsymbol{\theta}^n)$

Agent 1          Agent 2     ...     Agent n

  $a^1$   $o^1$         $a^2$   $o^2$            $a^n$   $o^n$
    ↓      ↑              ↓      ↑                ↓      ↑

                        Environment